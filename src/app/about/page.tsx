import React from "react";
import Trips from "./components/Trips";

export const metadata = {
  title: "About",
};

const about = () => {
  return (
    <div>
      about ué?
      <Trips />
    </div>
  );
};

export default about;
